apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: hyperspace
spec:
  serviceName: "hyperspace-headless"
  replicas: 3
  selector:
    matchLabels:
      app: hyperspace
  template:
    metadata:
      labels:
        app: hyperspace
    spec:
      containers:
        - name: db
          image: glukhota/hyperspace-db:latest
          ports:
            - containerPort: 50051 # gRPC
            - containerPort: 50050 # Dashboard
          volumeMounts:
            - name: data
              mountPath: /app/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 100Gi

name: hyperspacedb
description: Official Dart/Flutter SDK for HyperspaceDB. A hyper-fast, lock-free Vector Database built in Rust.
version: 3.1.6
repository: https://github.com/YARlabs/hyperspace-db

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  grpc: ^5.1.0
  protobuf: ^6.0.0
  fixnum: ^1.1.0
  pointycastle: ^3.9.1
  crypto: ^3.0.3

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

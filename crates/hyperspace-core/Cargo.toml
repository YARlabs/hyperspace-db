[package]
name = "hyperspace-core"
version = "1.1.0"
edition = "2021"

[dependencies]
thiserror = { workspace = true }
rand = { workspace = true }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "metric_bench"
harness = false
